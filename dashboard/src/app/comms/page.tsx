"use client";
/**
 * Comms Page — Phase 7
 *
 * Client-side comm log with master-detail layout,
 * filtering, and message inspection.
 */

import CommLog from "@/components/comm-log/CommLog";

export default function CommsPage() {
  return <CommLog />;
}
